"use client";

import * as React from "react";

export function SignInTokenClient() {
  React.useEffect(() => {
    // This page exists for Portal parity. TraderLaunchpad uses Clerk auth host flows.
  }, []);

  return null;
}

