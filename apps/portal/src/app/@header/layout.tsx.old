import { headers } from "next/headers";
import Link from "next/link";

import {
  Sidebar,
  SidebarContent,
  SidebarHeader,
  SidebarTrigger,
  SidebarTriggerWrapper,
} from "@acme/ui/components/sidebar";
import { Logo } from "@acme/ui/general/Logo";
import { NavUser } from "@acme/ui/general/nav-user";
import { TeamSwitcher } from "@acme/ui/general/team-switcher";
import TopNavbar from "@acme/ui/general/TopNavbar";
import { Separator } from "@acme/ui/separator";

// function isMondayFrame() {
//   const headersList = headers();
//   const referer = headersList.get("referer") ?? "";
//   return referer.includes("monday.com");
// }

export default function HeaderLayout({
  children,
  appName,
  sidebarToggle = true,
  className,
}: {
  children: React.ReactNode;
  appName: string;
  sidebarToggle: boolean;
  className?: string;
}) {
  // if (isMondayFrame()) {
  //   return null;
  // }

  return (
    // <Sidebar>
    <header className="group-has-data-[collapsible=icon]/sidebar-wrapper:h-14 sticky left-0 right-0 top-2 z-50 flex h-auto shrink-0 flex-col border-b bg-white transition-[width,height] ease-linear">
      <TopHeader sidebarToggle={sidebarToggle} />
      <MainHeader />
      <Breadcrumb />
    </header>
  );
}

const TopHeader = ({ sidebarToggle }: { sidebarToggle: boolean }) => {
  return (
    <div className="flex h-14 w-full items-center gap-1 px-4 lg:gap-2 lg:px-6">
      {sidebarToggle ? <SidebarTrigger className="-ml-1" /> : null}
      <Separator
        orientation="vertical"
        className="mx-2 data-[orientation=vertical]:h-4"
      />
      <Link href="/" className="flex items-center gap-2">
        <Logo appName="Portal Demo" />
      </Link>
      <div className="flex flex-1 items-center justify-end gap-5">
        <TopNavbar />
        <NavUser className="!ml-auto" />
      </div>
    </div>
  );
};

const MainHeader = () => {
  return (
    <div className="flex h-24 items-center bg-purple-500 px-4 lg:gap-2 lg:px-6">
      Main Header
    </div>
  );
};

const Breadcrumb = () => {
  return (
    <div className="flex h-10 items-center px-4 lg:gap-2 lg:px-6">
      Breadcrumb
    </div>
  );
};
