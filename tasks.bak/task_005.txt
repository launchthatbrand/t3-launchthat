# Task ID: 5
# Title: Implement Sidebar Navigation
# Status: pending
# Dependencies: 3, 4
# Priority: high
# Description: Create the sidebar navigation with Add New functionality and hierarchical display of Boards and Folders
# Details:
Enhance the Sidebar component to include:

1. "Add New" section with a "+" icon that opens a dropdown menu for creating Boards or Folders
2. Hierarchical tree-like display of Boards and Folders
3. Expand/collapse functionality for Folders
4. Visual indication of the currently selected Board or Folder

```tsx
// components/layout/Sidebar.tsx
import { FC, useState } from 'react';
import { useQuery } from 'convex/react';
import { api } from '../../convex/_generated/api';
import { Button } from '@ui/components/ui/button';
import { PlusIcon, FolderIcon, LayoutIcon, ChevronRightIcon, ChevronDownIcon } from 'lucide-react';
import { 
  DropdownMenu, 
  DropdownMenuContent, 
  DropdownMenuItem, 
  DropdownMenuTrigger 
} from '@ui/components/ui/dropdown-menu';

const Sidebar: FC = () => {
  const [collapsed, setCollapsed] = useState(false);
  const [expandedFolders, setExpandedFolders] = useState<Set<string>>(new Set());
  const [selectedItem, setSelectedItem] = useState<{type: 'board' | 'folder', id: string} | null>(null);
  
  const folders = useQuery(api.folders.list) || [];
  const boards = useQuery(api.boards.list) || [];
  
  const toggleFolder = (folderId: string) => {
    const newExpanded = new Set(expandedFolders);
    if (newExpanded.has(folderId)) {
      newExpanded.delete(folderId);
    } else {
      newExpanded.add(folderId);
    }
    setExpandedFolders(newExpanded);
  };
  
  // Render functions for boards and folders
  // Add New dropdown implementation
  // Selection handling
  
  return (
    <aside className={`border-r bg-gray-50 flex flex-col ${collapsed ? 'w-14' : 'w-64'} transition-all`}>
      {/* Sidebar content */}
    </aside>
  );
};

export default Sidebar;
```

# Test Strategy:
Test the sidebar navigation by creating multiple boards and folders and verifying they appear correctly in the hierarchy. Test the expand/collapse functionality for folders. Verify that the selected item is visually highlighted. Test the "Add New" functionality to ensure it correctly triggers the creation of boards and folders.
