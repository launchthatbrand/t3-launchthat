import { defineComponent } from "convex/server";

// Placeholder component definition (implementation added in later todos).
const component = defineComponent("launchthat_notifications");

export default component;

